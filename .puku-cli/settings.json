{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) .venv/bin/python -m ruff format \"$f\" >/dev/null 2>&1 || true ;; *) ;; esac; }",
            "statusMessage": "Running ruff format..."
          }
        ]
      }
    ]
  }
}
